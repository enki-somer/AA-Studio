/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: 'export',
  basePath: '/AA-Studio',
  images: {
    unoptimized: true,
  },
};

module.exports = nextConfig;
